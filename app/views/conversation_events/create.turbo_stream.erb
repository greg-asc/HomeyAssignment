<%= turbo_stream.prepend("conversation_events_#{@project.id}", render(ConversationEventPresenter.new(@form.event).component)) %>

<%= turbo_stream.replace("project_header_#{@project.id}", render(ProjectHeaderComponent.new(@project))) %>
<%= turbo_stream.replace("new_event_#{@project.id}", render(ConversationEventFormComponent.new(project: @project, user: current_user, form: nil))) %>
